#include<stdio.h>
#include<conio.h>

int main(){

int a,b;
printf("Enter 2 values");
scanf("%d %d", &a,&b);

if (a>b);
    print("%d > %d\n",a,b);
else
if (a<b);
    printf("%d < %d",b,a);
else
   printf("%d = %d",a,b);

   getch();

return 0:
}